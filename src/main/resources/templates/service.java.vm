package ${package.Service};

import ${package.DTO}.${entity}DTO;

/**
 * $!{table.comment} 服务类
 * @author ${author}
 * @since ${date}
 */
public interface ${entity}Service extends BaseService<${entity}DTO> {
}